{
  "name": "mml",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "packageManager": "pnpm@10.15.0",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "mml": "pnpm --filter @mml/cli start",
    "mcp": "pnpm --filter @mml/mcp start"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^24.0.0"
  }
}
